import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
  container: {
    flex: '1 0 auto',
    display: 'flex',
    flexDirection: 'column',
    width: '100%',
    padding: 24,
    alignSelf: 'center',
  },
}));
