import { makeStyles } from '@material-ui/core/styles';

export default makeStyles({
  container: {
    position: 'relative',
    width: '100%',
    display: 'flex',
    alignItems: 'center',
    paddingBottom: 24,
    left: -8,
  },
  label: {
    fontSize: 16,
    marginLeft: 3,
    cursor: 'pointer',
  },
});
